{
  "version": 2,
  "buildCommand": "cd web-app && npm install && npm run build",
  "outputDirectory": "web-app/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ],
  "functions": {
    "api/**/*.py": {
      "runtime": "@vercel/python"
    }
  }
}

